---
title: "Saucy Integration"
description: "Integration patterns for Saucy plugin fishing functionality in SND macros"
version: "1.0.0"
category: "plugins"
tags: ["saucy", "fishing", "plugin", "snd"]
dependencies: ["../core/character-conditions.mdc", "../core/error-handling.mdc", "../utilities/inventory-management.mdc"]
alwaysApply: false
---

# Saucy Integration

## Purpose
Integration patterns for Saucy plugin fishing functionality in SND macros.

## Scope
This rule covers Saucy fishing patterns, fishing state management, and fishing error handling.

## Basic Saucy Integration

### Plugin Availability Check
```lua
-- Check if Saucy is available
if not HasPlugin("Saucy") then
    yield("/echo [Script] Saucy plugin not available")
    StopFlag = true
    return
end
```

### Basic Fishing Patterns
```lua
-- Check if Saucy is running
if IPC.Saucy.IsRunning() then
    yield("/echo [Script] Saucy is already running")
    -- Handle already running state
end

-- Start fishing
IPC.Saucy.StartFishing()

-- Wait for fishing to complete
while IPC.Saucy.IsRunning() do
    yield("/wait 1")
end
```

## Advanced Fishing Patterns

### Fishing State Management
```lua
-- Check if Saucy is running
function IsSaucyRunning()
    return IPC.Saucy.IsRunning()
end

-- Wait for Saucy to complete
function WaitForSaucyComplete(timeout)
    timeout = timeout or 300 -- 5 minutes default
    local startTime = os.clock()
    
    while IsSaucyRunning() and (os.clock() - startTime) < timeout do
        yield("/wait 1")
    end
    
    if IsSaucyRunning() then
        yield("/echo [Script] Saucy timeout, stopping")
        IPC.Saucy.StopFishing()
        return false
    end
    
    return true
end
```

### Fishing Control Functions
```lua
-- Start/Stop fishing
if not IPC.Saucy.IsRunning() then
    IPC.Saucy.StartFishing()
end

-- Stop fishing
if IPC.Saucy.IsRunning() then
    IPC.Saucy.StopFishing()
end
```

## Fishing Workflow Patterns

### Complete Fishing Workflow
```lua
-- Complete fishing workflow
function CompleteFishingWorkflow(timeout)
    timeout = timeout or 300
    
    -- Check prerequisites
    if not HasPlugin("Saucy") then
        yield("/echo [Script] ERROR: Saucy plugin not available")
        return false
    end
    
    if not Player.Available then
        yield("/echo [Script] ERROR: Player not available")
        return false
    end
    
    -- Check if already running
    if IsSaucyRunning() then
        yield("/echo [Script] Saucy is already running")
        return true
    end
    
    -- Start fishing
    yield("/echo [Script] Starting fishing")
    IPC.Saucy.StartFishing()
    
    -- Wait for completion
    if WaitForSaucyComplete(timeout) then
        yield("/echo [Script] Fishing completed successfully")
        return true
    else
        yield("/echo [Script] Fishing failed or timed out")
        return false
    end
end
```

### Fishing with Retry
```lua
-- Fishing with retry logic
function FishWithRetry(maxRetries, timeout)
    maxRetries = maxRetries or 3
    timeout = timeout or 300
    
    for attempt = 1, maxRetries do
        if CompleteFishingWorkflow(timeout) then
            return true
        end
        
        if attempt < maxRetries then
            yield("/echo [Script] Fishing attempt " .. attempt .. " failed, retrying")
            yield("/wait 5")
        end
    end
    
    yield("/echo [Script] Fishing failed after " .. maxRetries .. " attempts")
    return false
end
```

## State Machine Integration

### Fishing State Pattern
```lua
-- Add fishing state to your state machine
CharacterState = {
    ready = Ready,
    fishing = Fishing,
    -- ... other states
}

function Fishing()
    if CompleteFishingWorkflow(300) then
        yield("/echo [Script] Fishing completed")
        State = CharacterState.ready
    else
        yield("/echo [Script] Fishing failed")
        State = CharacterState.ready
    end
end
```

### Fishing with Inventory Check
```lua
-- Fishing with inventory management
function FishingWithInventoryCheck(timeout)
    -- Check inventory space before fishing
    if Inventory.GetFreeInventorySlots() <= 5 then
        yield("/echo [Script] Not enough inventory space for fishing")
        return false
    end
    
    -- Start fishing
    if not CompleteFishingWorkflow(timeout) then
        return false
    end
    
    -- Check inventory after fishing
    yield("/echo [Script] Fishing completed, inventory slots: " .. Inventory.GetFreeInventorySlots())
    return true
end
```

## Error Handling

### Saucy Error Handling
```lua
-- Safe Saucy function call
function SafeSaucyCall(functionName, ...)
    if not HasPlugin("Saucy") then
        return nil, "Saucy plugin not available"
    end
    
    local success, result = pcall(function()
        return IPC.Saucy[functionName](...)
    end)
    
    if success then
        return result, nil
    else
        return nil, "Saucy call failed: " .. tostring(result)
    end
end
```

### Fishing Timeout Handling
```lua
-- Handle fishing timeout
local startTime = os.clock()
while IsSaucyRunning() and (os.clock() - startTime) < 300 do
    yield("/wait 1")
end

if IsSaucyRunning() then
    yield("/echo [Script] WARNING: Fishing taking too long, stopping...")
    IPC.Saucy.StopFishing()
end
```

## Configuration Integration

### Saucy Configuration Variables
```lua
-- Add to your macro's configs section
configs:
  EnableSaucy:
    default: true
    description: Enable Saucy fishing
  FishingTimeout:
    default: 300
    description: Fishing timeout in seconds
  MaxFishingRetries:
    default: 3
    description: Maximum fishing retry attempts
  MinInventorySlots:
    default: 5
    description: Minimum free inventory slots required for fishing
```

### Usage in Code
```lua
-- Get configuration values
local enableSaucy = Config.Get("EnableSaucy") == "true"
local fishingTimeout = tonumber(Config.Get("FishingTimeout"))
local maxRetries = tonumber(Config.Get("MaxFishingRetries"))
local minInventorySlots = tonumber(Config.Get("MinInventorySlots"))

-- Use in fishing logic
if enableSaucy and HasPlugin("Saucy") then
    if Inventory.GetFreeInventorySlots() >= minInventorySlots then
        if not FishWithRetry(maxRetries, fishingTimeout) then
            yield("/echo [Script] Fishing failed")
            return false
        end
    else
        yield("/echo [Script] Not enough inventory space for fishing")
    end
end
```

## Character Condition Integration

### Fishing Mode Integration
```lua
-- Check if character is in fishing mode
if Svc.Condition[CharacterCondition.fishingMode] then
    yield("/echo [Script] Character is in fishing mode")
    -- Handle fishing mode logic
end
```

### Character Busy Check with Saucy
```lua
-- Check if character is busy (including Saucy)
function IsCharacterBusy()
    return Svc.Condition[CharacterCondition.casting] or
           Svc.Condition[CharacterCondition.betweenAreas] or
           Svc.Condition[CharacterCondition.beingMoved] or
           Svc.Condition[CharacterCondition.fishingMode] or
           IsSaucyRunning() or
           Player.IsBusy
end
```

## Best Practices

### 1. Always Check Plugin Availability
```lua
-- Check if Saucy is available before use
if not HasPlugin("Saucy") then
    yield("/echo [Script] Saucy not available")
    return false
end
```

### 2. Use Timeouts for Fishing
```lua
-- Always use timeouts for fishing operations
if not WaitForSaucyComplete(300) then
    yield("/echo [Script] Fishing timeout")
    return false
end
```

### 3. Handle Fishing States
```lua
-- Check fishing state before starting new fishing
if IsSaucyRunning() then
    yield("/echo [Script] Saucy is already running")
    return true
end
```

### 4. Check Inventory Space
```lua
-- Check inventory space before fishing
if Inventory.GetFreeInventorySlots() <= 5 then
    yield("/echo [Script] Not enough inventory space for fishing")
    return false
end
```

### 5. Use Appropriate Wait Times
```lua
-- Use appropriate wait times for fishing monitoring
while IsSaucyRunning() do
    yield("/wait 1") -- Longer wait for fishing
end
```

## Related Rules
- [Character Conditions](../core/character-conditions.mdc) - Character condition checking for fishing
- [Error Handling](../core/error-handling.mdc) - Error handling patterns
- [State Machines](../core/state-machines.mdc) - State machine integration
- [Inventory Management](../utilities/inventory-management.mdc) - Inventory management utilities
- [Rule Management](../management/rule-management.mdc) - Rule organization guidelines

## Rule Dependencies
This rule extends:
- [Character Conditions](../core/character-conditions.mdc) - Uses character conditions for fishing state checking
- [Error Handling](../core/error-handling.mdc) - Uses error handling patterns for fishing operations
- [State Machines](../core/state-machines.mdc) - Uses state machine patterns for fishing integration